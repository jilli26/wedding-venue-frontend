import React from 'react'
import { Button } from 'semantic-ui-react'

const ButtonExampleFluid = (props) => (
  <Button fluid onClick={props.handleBooking}>Book</Button>
)

export default ButtonExampleFluid
